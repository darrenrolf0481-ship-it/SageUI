<?xml version="1.0" encoding="utf-8"?>
<!--
  PLACE AT: android/app/src/main/res/xml/network_security_config.xml

  Then in AndroidManifest.xml <application> tag add:
    android:networkSecurityConfig="@xml/network_security_config"

  This allows cleartext (http://) to localhost and LAN IPs
  so SAGE Bridge (port 8000) and Ollama (port 11434) work.
-->
<network-security-config>
  <!-- Allow cleartext to localhost for Ollama / SAGE Bridge -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">localhost</domain>
    <domain includeSubdomains="true">10.0.0.0/8</domain>
    <domain includeSubdomains="true">192.168.0.0/16</domain>
    <domain includeSubdomains="true">172.16.0.0/12</domain>
    <domain includeSubdomains="true">127.0.0.1</domain>
  </domain-config>
  <!-- All other traffic requires HTTPS (Gemini API etc.) -->
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <certificates src="system" />
    </trust-anchors>
  </base-config>
</network-security-config>
